import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const backgroundDark = Color(0xFF222831); // Deep dark blue-gray
  static const backgroundLightDark = Color(0xFF393E46); // Slightly lighter dark
  static const backgroundCard = Color(0xFF23272F); // Card background
  static const backgroundLight = Color(0xFFF5F5F5); // Light background
  static const backgroundLightCard = Color(0xFFFFFFFF); // Light card background

  // Accents
  static const accentAqua = Color(0xFF00ADB5); // Vibrant aqua
  static const accentYellow = Color(0xFFFFC107); // For highlights
  static const accentRed = Color(0xFFE53935); // For errors or alerts
  static const accentGreen = Color(0xFF43A047); // For success

  // Text
  static const textPrimary = Color(0xFFFFFFFF); // White
  static const textSecondary = Color(0xFFB0BEC5); // Muted gray
  static const textTitle = Color(0xFFF5F5F5); // Slightly off-white
  static const textPrimaryLight = Color(0xFF222831); // Dark text for light mode
  static const textSecondaryLight = Color(
    0xFF666666,
  ); // Muted text for light mode

  // Divider
  static const divider = Color(0x33FFFFFF); // Subtle divider
}
